/**
 * Comparison Section Component
 * Collapsible section wrapper for comparison sections
 * (Currently used as a type reference, actual implementation in ComparisonTable)
 */

export interface ComparisonSectionData {
  id: string;
  title: string;
  description?: string;
  defaultOpen?: boolean;
}



